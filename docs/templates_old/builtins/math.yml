name: Math
description: Math block or inline.

type: shortcode
shortcode:
  kind: block
  parameters: []


templates:
  html: !String |
    ${{ body }}$
  markdown: !String |
    ${{body}}$
  latex: !String |
    ${{ body }}$