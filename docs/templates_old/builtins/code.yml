name: Inline code
description: Inline code.

type: shortcode
shortcode:
  kind: block
  parameters: []


templates:
  html: !String |
    <code>{{ body }}</code>
  markdown: !String |
    `{{body}}`
  latex: !String |
    \mintinline{ {{body}} }