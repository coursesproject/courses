name: Paragraph
description: A paragraph.

type: shortcode
shortcode:
  kind: block
  parameters: []


templates:
  html: !Precompiled [paragraph, html]
  markdown: !String |
    {{ body | safe }}\n
  latex: !String |
    {{ body | safe }}\n