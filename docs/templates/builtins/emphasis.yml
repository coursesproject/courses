name: Emphasis
description: Text emphasis.

type: shortcode
shortcode:
  kind: block
  parameters: []


templates:
  html: !String |
    <em>{{body | safe}}</em>
  markdown: !String |
    _{{body | safe }}_
  latex: !String |
    \emph{ {{body | safe }} }