name: Underline
description: Underlined text.

type: builtin


templates:
  html: !String |
    <u>{{ value | safe }}</u>
  markdown: !String |
    <ins>{{ value | safe }}<ins>
  latex: !String |
    \underline{ {{value | safe}} }