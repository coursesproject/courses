name: Strong
description: Strong/bold text.

type: shortcode
shortcode:
  kind: block
  parameters: []


templates:
  html: !String |
    <strong>{{ body | safe }}</strong>
  markdown: !String |
    __{{ body | safe }}__
  latex: !String |
    \textbf{ {{body | safe}} }