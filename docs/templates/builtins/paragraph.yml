name: Paragraph
description: A paragraph.

type: builtin


templates:
  html: !String |
    <p>{{ value | safe }}</p>
  markdown: !String |
    {{ value | safe }}\n
  latex: !String |
    {{ value | safe }}\n